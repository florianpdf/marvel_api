{% if character is defined %}
    {% for char in character['data']['results'] %}
        Nom: {{ char.name }}
        Image: <img src="{{ char.thumbnail.path }}.{{ char.thumbnail.extension }}" alt="">
    {% endfor %}
{% endif %}